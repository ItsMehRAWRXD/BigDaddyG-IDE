# BigDaddyG Agent Configuration
# Copy this file to .env and configure as needed

# Sandbox daemon endpoint (Unix socket or TCP)
# Default: unix:///var/run/bigdaddyg-sandbox.sock
# Alternative: tcp://localhost:8080
AGENT_SANDBOX_ENDPOINT=unix:///var/run/bigdaddyg-sandbox.sock

# Database path (optional, defaults to agent.db in current directory)
# AGENT_DB_PATH=/path/to/agent.db