/**
 * Settings Importer - Minimal stub
 */

class SettingsImporter {
  constructor(marketplaceClient, extensionHost) {
    this.marketplaceClient = marketplaceClient;
    this.extensionHost = extensionHost;
  }
}

module.exports = SettingsImporter;